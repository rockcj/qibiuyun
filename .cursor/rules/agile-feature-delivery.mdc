---
description: 敏捷式功能拆分、优先级排序、测试通过后提交规范
alwaysApply: true
---

# 敏捷式开发与提交规范

## 开发原则

- 开发过程必须采用敏捷式增量交付，每次只实现一个可独立验证的细分功能模块。
- 所有功能必须按必要程度和重要性排序，优先完成 P0，再做 P1，最后做 P2。
- 后续功能未完成时，不能影响已完成 P0/P1 功能的正常使用。
- 不允许一次性混入多个无关功能、重构、格式化或文档变更。

## 优先级定义

- P0：演示闭环必需功能，缺失后产品无法跑通主流程。
- P1：增强核心体验的功能，缺失后主流程仍可使用。
- P2：锦上添花功能，缺失后不影响比赛演示和主流程稳定性。

## 功能拆分要求

- 每个功能必须拆成可测试、可提交、可回滚的小模块。
- 每个模块必须有明确入口、输入、输出和验收标准。
- 如果一个功能依赖后续模块，必须提供可运行兜底或 Mock，不得阻塞前序功能。
- 前端、后端、接口、测试文档应保持同步更新。

## 开发流程

1. 明确当前功能的业务目标和优先级。
2. 拆分最小可交付模块，并确认不破坏已有功能。
3. 先补充或明确测试用例与验收标准。
4. 实现当前模块，保持改动范围最小。
5. 运行相关测试和必要的手动验证。
6. 测试通过后才允许暂存、提交和推送。
7. 测试失败时必须先修复，禁止提交失败状态。

## 提交限制

- 未通过测试不允许提交。
- 未说明测试方式不允许创建 PR。
- 未确认前序功能仍可使用不允许合并。
- 修复失败测试时，只改当前功能相关代码，避免扩大变更范围。
