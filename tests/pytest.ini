[pytest]
# OfferGPT 后端测试配置
asyncio_mode = auto
testpaths = backend
pythonpath = ../backend
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: 跨模块集成测试，依赖内存数据库
    contract: API 契约一致性测试
    p0: P0 面试场景必测用例
